class weather:
	def __init__( self, label, lineEdit, radio1, radio2, button, linkLabel ):
		self.cityLabel = label
		self.lineEdit = lineEdit
		self.celsius = radio1
		self.fahrenheit = radio2
		self.submit = button
		self.linkLabel = linkLabel

	no need for this, only when submit clicked write to file
	